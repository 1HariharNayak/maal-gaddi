// 8px grid spacing scale.
// Import this everywhere instead of writing raw numbers like `padding: 16`.
// Example: padding: Spacing.md

const Spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,

  // Shared shape tokens live here too since they're layout-related.
  borderRadius: 16,
};

export default Spacing;
