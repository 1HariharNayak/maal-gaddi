// Typography scale. Reuse these sizes instead of one-off fontSize values.
// Example: fontSize: Fonts.h1

const Fonts = {
  h1: 28,
  h2: 22,
  h3: 18,
  body: 14,
  caption: 12,

  // Weight tokens kept alongside size so text style objects read cleanly.
  weight: {
    regular: "400",
    medium: "500",
    semibold: "600",
    bold: "700",
  },
};

export default Fonts;
